---
import './live-cursors'

export interface Props {
  movement?: 'quick' | 'smooth' | 'perfect';
  selector?: string;
  names?: boolean;
}

const { movement, selector, names } = Astro.props
---

<live-cursor movement={movement} selector={selector} names={names} client:load>
  <slot></slot>
</live-cursor>
