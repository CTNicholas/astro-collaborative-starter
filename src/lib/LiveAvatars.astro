---
import './live-avatars'
import type { UserShow } from '../types'

export interface Props {
  size?: number | string;
  show?: UserShow;
}

const { size, show } = Astro.props
---

<live-avatars size={size} show={show} client:load></live-avatars>
