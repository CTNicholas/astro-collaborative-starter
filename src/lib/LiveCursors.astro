---
import './live-cursors'
const { movement, selector, element } = Astro.props
---

<live-cursor movement={movement} selector={selector} element={element} client:load>
  <slot></slot>
</live-cursor>
