---
import './live-cursors'

export interface Props {
  movement?: 'quick' | 'smooth' | 'perfect';
  selector?: string;
}

const { movement, selector } = Astro.props
---

<live-cursor movement={movement} selector={selector} client:load>
  <slot></slot>
</live-cursor>
